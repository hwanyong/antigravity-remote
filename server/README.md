# Antigravity Remote Bridge — Daemon Server

## Quick Start

```bash
cd server/
python -m venv .venv
source .venv/bin/activate
pip install -e .
agbridge-server
```

## Architecture

See `implementation_plan_ko.md` / `implementation_plan_en.md` in the project artifacts.
